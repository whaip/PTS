# 电阻测试整个流程图

## 流程概述
电阻测试流程基于新的组件诊断框架（ComponentDiagnosticFramework），以调用的函数为节点展示完整的测试链路。

## 详细流程图

```mermaid
flowchart TD
    Start([开始电阻测试]) --> A1[FaultDiagnostic::diagnoseComponent]
    
    A1 --> A2[ComponentDiagnosticManager::diagnoseComponent]
    A2 --> A3[ComponentDiagnosticManager::executeSingleDiagnosis]
    A3 --> A4[BaseComponentDiagnostic::diagnoseComponent]
    
    A4 --> B1[验证组件规格]
    B1 --> B1_1[ResistorDiagnostic::validateComponentSpec]
    B1_1 --> B1_2{验证通过?}
    B1_2 -->|否| Error1[抛出异常: 组件规格验证失败]
    B1_2 -->|是| B2[分配端口]
    
    B2 --> B2_1[ResistorDiagnostic::getPortRequirements]
    B2_1 --> B2_2[BaseComponentDiagnostic::allocatePorts]
    B2_2 --> B2_3{端口分配成功?}
    B2_3 -->|否| Error2[抛出异常: 端口分配失败]
    B2_3 -->|是| B3[设置接线]
    
    B3 --> B3_1[ResistorDiagnostic::generateWiringScheme]
    B3_1 --> B3_2[BaseComponentDiagnostic::setupWiring]
    B3_2 --> B3_3[emit wiringRequired信号]
    B3_3 --> B3_4[等待用户接线完成]
    B3_4 --> B3_5[emit wiringCompleted信号]
    B3_5 --> B3_6{接线完成?}
    B3_6 -->|否| Error3[抛出异常: 接线设置失败]
    B3_6 -->|是| B4[预处理设置]
    
    B4 --> B4_1[ResistorDiagnostic::preTestSetup]
    B4_1 --> B4_2{预处理成功?}
    B4_2 -->|否| Error4[抛出异常: 预处理设置失败]
    B4_2 -->|是| B5[配置数据采集]
    
    B5 --> B5_1[ResistorDiagnostic::configureDataAcquisition]
    B5_1 --> B5_2[ResistorDiagnostic::calculateOptimalTestParams]
    B5_2 --> B5_3[配置端口参数]
    B5_3 --> B5_4[生成ComponentTestConfig]
    B5_4 --> B6[执行数据采集]
    
    B6 --> B6_1[emit testStarted信号]
    B6_1 --> B6_2[ResistorDiagnostic::executeDataAcquisition]
    B6_2 --> B6_3[配置设备电流源]
    B6_3 --> B6_4[循环测量电压电流]
    B6_4 --> B6_5[计算平均值和电阻值]
    B6_5 --> B6_6[生成TestData]
    B6_6 --> B6_7[emit testCompleted信号]
    B6_7 --> B6_8[BaseComponentDiagnostic::validateTestData]
    B6_8 --> B6_9{数据有效?}
    B6_9 -->|否| Error5[抛出异常: 测试数据验证失败]
    B6_9 -->|是| B7[故障分析]
    
    B7 --> B7_1[ResistorDiagnostic::analyzeFaults]
    B7_1 --> B7_2[提取测量数据]
    B7_2 --> B7_3[ResistorDiagnostic::calculateResistance]
    B7_3 --> B7_4[ResistorDiagnostic::isOpenCircuit]
    B7_4 --> B7_5[ResistorDiagnostic::isShortCircuit]
    B7_5 --> B7_6[ResistorDiagnostic::isOutOfTolerance]
    B7_6 --> B7_7[计算健康评分和置信度]
    B7_7 --> B7_8[生成ComponentDiagnosticResult]
    B7_8 --> B8[后处理清理]
    
    B8 --> B8_1[ResistorDiagnostic::postTestCleanup]
    B8_1 --> B8_2[BaseComponentDiagnostic::releasePorts]
    B8_2 --> B9[结果转换]
    
    B9 --> B9_1[FaultDiagnostic::convertFromComponentDiagnosticResult]
    B9_1 --> B9_2[emit diagnosisCompleted信号]
    B9_2 --> End([测试完成])
    
    %% 异常处理路径
    Error1 --> ErrorHandle[异常处理]
    Error2 --> ErrorHandle
    Error3 --> ErrorHandle
    Error4 --> ErrorHandle
    Error5 --> ErrorHandle
    ErrorHandle --> ErrorResult[生成错误结果]
    ErrorResult --> End
    
    %% 进度信号
    B1_2 --> Progress1[emit diagnosisProgress 10%]
    B2_3 --> Progress2[emit diagnosisProgress 20%]
    B3_6 --> Progress3[emit diagnosisProgress 40%]
    B4_2 --> Progress4[emit diagnosisProgress 50%]
    B5_4 --> Progress5[emit diagnosisProgress 60%]
    B6_9 --> Progress6[emit diagnosisProgress 80%]
    B7_8 --> Progress7[emit diagnosisProgress 90%]
    B8_2 --> Progress8[emit diagnosisProgress 100%]
```

## 关键函数调用链

### 1. 主入口函数
- `FaultDiagnostic::diagnoseComponent(ComponentSpec)`
- `ComponentDiagnosticManager::diagnoseComponent(ComponentSpec)`
- `BaseComponentDiagnostic::diagnoseComponent(ComponentSpec)`

### 2. 电阻器专用诊断器函数
- `ResistorDiagnostic::validateComponentSpec()` - 验证组件规格
- `ResistorDiagnostic::getPortRequirements()` - 获取端口需求
- `ResistorDiagnostic::generateWiringScheme()` - 生成接线方案
- `ResistorDiagnostic::preTestSetup()` - 预测试设置
- `ResistorDiagnostic::configureDataAcquisition()` - 配置数据采集
- `ResistorDiagnostic::executeDataAcquisition()` - 执行数据采集
- `ResistorDiagnostic::analyzeFaults()` - 故障分析

### 3. 测试参数计算函数
- `ResistorDiagnostic::calculateOptimalTestParams()` - 计算最优测试参数

### 4. 数据采集核心函数
- `ResistorDiagnostic::calculateResistance()` - 计算电阻值

### 5. 故障检测函数
- `ResistorDiagnostic::isOpenCircuit()` - 开路检测
- `ResistorDiagnostic::isShortCircuit()` - 短路检测  
- `ResistorDiagnostic::isOutOfTolerance()` - 容差检测

### 6. 后处理函数
- `ResistorDiagnostic::postTestCleanup()` - 测试后清理
- `BaseComponentDiagnostic::releasePorts()` - 释放端口

### 7. 结果转换函数
- `FaultDiagnostic::convertFromComponentDiagnosticResult()` - 结果格式转换

## 测试数据流

### 输入数据
- `ComponentSpec` 组件规格（标称值、容差、引脚等）

### 中间数据
- `PortRequirement[]` 端口需求列表
- `WiringConnection[]` 接线连接方案
- `ComponentTestConfig` 测试配置参数
- `TestData` 原始测试数据（电压、电流、时间戳）

### 输出数据
- `ComponentDiagnosticResult` 诊断结果
- `DiagnosticResult` 兼容格式结果

## 信号流

### 进度信号
- `diagnosisStarted(componentId)` - 诊断开始
- `diagnosisProgress(componentId, percentage)` - 诊断进度
- `diagnosisCompleted(componentId, result)` - 诊断完成

### 接线信号
- `wiringRequired(componentId, connections)` - 需要接线
- `wiringCompleted(componentId)` - 接线完成

### 测试信号
- `testStarted(componentId)` - 测试开始
- `testCompleted(componentId, testData)` - 测试完成

## 异常处理

所有关键步骤都有异常检查和处理机制：
1. 组件规格验证失败
2. 端口分配失败
3. 接线设置失败
4. 预处理设置失败
5. 测试数据验证失败

异常发生时会：
- 释放已分配的资源
- 生成错误结果
- 发出错误信号
- 返回错误状态

## 测试流程特点

1. **模块化设计** - 每个步骤由专门的函数负责
2. **信号驱动** - 通过Qt信号系统进行进度通知
3. **异常安全** - 完善的错误处理和资源清理
4. **可扩展性** - 基于虚函数的继承体系
5. **向后兼容** - 保持与旧版本API的兼容
